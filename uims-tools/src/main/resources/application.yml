server:
  port: 8082

spring:
  application:
    name: uims-tools

logging:
  config: classpath:spring-logback.xml

report:
  pool:
    autoSetCorePool : true
    corePoolSize: 2
    maxPoolSize: 3
    keepAliveSeconds: 60
    queueCapacity: 500